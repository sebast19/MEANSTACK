import { BeautyRolePipe } from './beauty-role.pipe';

describe('BeautyRolePipe', () => {
  it('create an instance', () => {
    const pipe = new BeautyRolePipe();
    expect(pipe).toBeTruthy();
  });
});
